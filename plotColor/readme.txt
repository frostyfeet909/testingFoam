Place this folder in your working directory
The CSV file data.csv should be located at '../data.csv'
python plotColor.py

Might need to give executable premission to .run files.

WARNING: Only been tested on a small data set, may have problems when len(ep) != len(x), there is a measure in place but
its robustness has not been tested.
